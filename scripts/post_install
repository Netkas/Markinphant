<?php

use Markinphant\Classes\Factory;

require 'ncc';
    import('com.netkas.markinphant');

    print('Updating configuration file...' . PHP_EOL);

    try
    {
        Factory::getConfiguration();
        print('Done!' . PHP_EOL);
    }
    catch(Exception $e)
    {
        print(sprintf('Error: %s', $e->getMessage()) > PHP_EOL);
    }

